const CheckNums = (num1, num2) => {
  if (num1 < num2) console.log('true');
  else if (num1 > num2) console.log('false');
  else console.log('-1');
}

CheckNums(3, num2 = 2232);

CheckNums(num1 = 100, 3);

CheckNums(5, 5);
